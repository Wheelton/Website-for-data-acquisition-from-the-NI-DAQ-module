# Website-for-data-acquisition-from-the-NI-DAQ-module

## RUNNING LOCALLY USING **NI MAX**
- Install NI MAX
- Install Python
- Install `nidaqmx` Python NI DAQmx API:
  `py -m pip install nidaqmx`
- Install `matplotlib` Python Matlab API:
  `py -m pip install matplotlib`
